"""
Supported locales for the LLM application templates.
"""

from enum import Enum


class Locale(str, Enum):
    """
    Supported locales for the LLM application templates.
    """

    EN = "en"
    AR = "ar"
